//
//  find_new_words.cpp
//  find_new_words
//
//  Created by Timeless‘ on 15/5/21.
//  Copyright (c) 2015年 TsingHua. All rights reserved.
//

#include "find_new_words.h"
#include <string>
/*void find_words_from_a_file :: open_file()
{
    std :: cout << "PLease input the file you want to operate: ";
    std :: string fn1;     //The name of the file.
    std :: cin >> fn1;
    std :: ifstream infile(fn1);       //Create a ifstream.
    if(!infile)
    {
        std :: cout << "Error when open the file!" << std :: endl;
        return;
    }
}*/
bool find_words_from_a_file :: is_letter(const char& a)
{
    for(int i = 65; i <= 90; i += 1)
    {
        if(int(a) == i || int(a) == i + 25)
            return true;
    }
    return false;
}
/*bool find_words_from_a_file :: is_symble(const char &a)
{
    std :: string symble_union = " "
}*/

void find_words_from_a_file :: clear(std::string &s)
{
    s = "";
}
